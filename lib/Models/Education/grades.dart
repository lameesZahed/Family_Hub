import 'package:flutter/material.dart';
class Grades{
  final String subjectName;
  final int maxGrade;
  final int currentGrade;
  const Grades({required this.subjectName,required this.maxGrade,required this.currentGrade});
}